1.服务端启动程序为server_open.py，需要先打开服务端
2.客户端启动程序为clent_entrance.py
3.客户端功能实现了：
    1.用户加密认证
    2.允许同时多用户登录
    3.每个用户有自己的家目录 ，且只能访问自己的家目录
    4.对用户进行磁盘配额，每个用户的可用空间不同
    5.允许用户在ftp server上随意切换目录
    6.允许用户查看当前目录下文件
    7.允许上传和下载文件，保证文件一致性(md5)
    8.文件传输过程中显示进度条
    9.附加功能：支持文件的断点续传

在客户端功能有：
              1.获取客户端文件 需要输入命令：get_server_file
              2.设置服务端目录：set_server_dir
              3.获取用户目录文件：get_myfile
              4.下载命令：get 文件名(xxx.jpg)   # get和文件名中间必须有一个空格 而且文件名中不允许有空格
              5.上传命令：put 文件名(xxx.jpg)   # put和文件名中间必须有一个空格 而且文件名中不允许有空格

用户使用前需要先注册，注册成功后默认在：多用户在线FTP程序\user_dir 目录下创建单独的用户文件夹，
上传和下载文件都在这文件夹里。

服务端一开始默认文件夹在：多用户在线FTP程序\服务端\share